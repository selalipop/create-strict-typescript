# {{name}}

A TypeScript library with a strict baseline.

## Scripts

- `{{pm}} lint` — Biome + Oxlint + typecheck
- `{{pm}} fix` — auto-fix lint issues
- `{{pm}} build` — build with tsdown (if enabled)
- `{{pm}} typecheck` — TypeScript type-check only
